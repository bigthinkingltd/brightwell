import { Cinzel, Quintessential } from 'next/font/google';

export const cinzel = Cinzel({
  subsets: ['latin'],
  weight: ['400', '600', '700'],
});

export const quintessential = Quintessential({
  subsets: ['latin'],
  weight: ['400'],
});